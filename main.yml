---
- hosts: all
  become: true
  vars: 
    ssh_use_pam: true
    jenkins_hostname: 3.248.228.93
  tasks:
    - name: Update & Upgrade
      shell: | 
        sudo apt update -y
        sudo apt upgrade -y
    - name: Create user account
      ansible.builtin.user:
        name: kula
        comment: Thulani Kula
        uid: 1040
        group: admin

      
